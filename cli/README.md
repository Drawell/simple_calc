# Run calculation in command line

Usage
```bash
cli.exe --expression="1 + 1"
```

Or when you want to wait some time
```bash
cli.exe --expression="1 + 1" --wait
```

It is waiting for len(expression) seconds